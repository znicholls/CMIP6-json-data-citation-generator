language: python
cache: pip
dist: trusty
python:
  - "2.7"

before_install:
  - pip install pip --upgrade

install:
  - pip install -e .[test]

script:
  - pytest --cov -rfsxEX

after_success:
  - codecov
